# DSR Response Preparation

`ADMIN` and `LAWYER` can prepare:

- `responseSummary`
- `responseDraft`
- `responseDecision`
- `decisionRationale`

The module does not send responses automatically. `responseDraft` and `decisionRationale` are CRM-only fields.

Audit metadata records flags and decision values, but not the full response body.
