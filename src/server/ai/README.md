# AI-ready module

This directory is reserved for future PRIVAZY AI features.

Planned usage:

- Vercel AI SDK for model orchestration and streaming where needed.
- OpenAI structured outputs validated by Zod schemas.
- Supabase `pgvector` for embeddings and RAG once the product needs retrieval.
- AI as an assistant layer only: analysis, suggestions, classification, missing-data detection, and draft support.

Final legal documents should continue to be generated from approved, versioned templates.
